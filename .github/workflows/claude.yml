name: Claude

on:
  # Comment events - respond to @claude mentions
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]
  
  # Manual trigger
  workflow_dispatch:
    inputs:
      pr_number:
        description: 'PR number to review'
        required: false

jobs:
  claude:
    # Only run if @claude is mentioned in a comment
    if: |
      github.event_name == 'workflow_dispatch' ||
      contains(github.event.comment.body, '@claude')
    
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      issues: write
      id-token: write
      actions: read
    
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Debug secrets
        run: |
          if [ -n "${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}" ]; then
            echo "CLAUDE_CODE_OAUTH_TOKEN is set (length: ${#OAUTH_TOKEN})"
          else
            echo "CLAUDE_CODE_OAUTH_TOKEN is NOT set"
          fi
        env:
          OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
      
      - name: Claude Code Action
        uses: anthropics/claude-code-action@v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          
          # Custom instructions - always read the docs first
          prompt: |
            Before responding, read and understand these project docs:
            - AGENTS.md (contributor rules)
            - docs/SPEC.md (what's in scope for M0/M1)
            - docs/STYLE.md (code style)
            
            For PRs: Check if changes are in scope and follow style guide.
            For issues: Help answer questions or clarify scope.
            For @claude mentions: Assist with whatever is asked.
            
            Be concise. Reference the docs when relevant.
